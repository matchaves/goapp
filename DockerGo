FROM golang:1.15-alpine

WORKDIR /go/src/app
ADD main.go .

RUN apk update && apk add git
RUN go get -d -v ./...
RUN go install -v ./...

#ENV BAR=teste_successful_environment

EXPOSE 32001

CMD ["app"]